<h1><%= @patient.id ? 'Edit Patient' : 'Create Patient' %></h1>
<hr />
<%= form_with(model: @patient) do |f| %>
  <%= f.label :full_name %>
  <%= f.text_field :patient_name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>
<h1><%= @physician.id ? "Edit Physician" : "Create New Physician" %></h1>
<hr/>
<%= form_with(model: @physician, local: true, class: 'ui form' ) do |f| %>
<div class="field">
    <%= f.label :dr_name %>
    <%= f.text_field :dr_name, required: true, autofocus: true, placeholder: "Name" %>
  </div>
<% end %>